# Data

Place the project datasets in this directory as:

- `train.csv`
- `test.csv`

Both files must contain a target column named `label`.

The CSV files are intentionally excluded from version control by `.gitignore`.
If the data are publicly distributable, replace this note with the source,
license, and download instructions.
